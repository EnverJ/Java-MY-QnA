package stream;

public class FilterTest {
   /* @Test
    public void testFilteringAgeList() {
        List<Integer> testAges = Arrays.asList(23, 24, 35, 67);
        int ageThreshold = 30;

        List<String> expected = Arrays.asList("35", "67 Older than 60");
        List<String> result = AgeFilter.filteringAgeList(testAges, ageThreshold);

        assertEquals(expected, result);
    }

    @Test
    public void testFilteringAgeListWithEmptyList() {
        List<Integer> testAges = Arrays.asList();
        int ageThreshold = 30;

        List<String> expected = Arrays.asList();
        List<String> result = AgeFilter.filteringAgeList(testAges, ageThreshold);

        assertEquals(expected, result);
    }*/

}
